<UserControl
    x:Class="FN.Framtal.Presentation._2004.Layouts.Layout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:navigation="clr-namespace:FN.Framtal.Presentation._2004.Navigation"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <navigation:NavigationBar Grid.Row="0" DataContext="{Binding NavigationBarViewModel}" />

        <ContentControl
            Grid.Row="1"
            MaxWidth="1000"
            Margin="20"
            Content="{Binding ContentViewModel}" />

    </Grid>
</UserControl>
